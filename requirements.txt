# TeleTrader dependencies

# Telegram client
telethon>=1.34.0

# MT5 trading API (Windows only)
MetaTrader5>=5.0.45

# Web dashboard
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Config
python-dotenv>=1.0.0

# ZeroMQ — signal broadcasting between service and traders
pyzmq>=26.0.0

# Browser automation (disabled — re-enable when calculator dropdown is fixed)
# playwright>=1.44.0
